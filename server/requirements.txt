# Core Framework
flask==3.0.3
flask-cors==4.0.1
python-dotenv==1.0.1

# Testing
requests==2.31.0

# Module A (Person 1) - PDF Parsing & URL Fetching
PyPDF2==3.0.1
beautifulsoup4==4.12.3
# Alternative: pdfplumber==0.11.0

# Module B (Person 2) - LLM Summarization
# Choose ONE of the following:
# openai==1.12.0        # For OpenAI GPT models
# anthropic==0.18.1     # For Claude models

# Module C (Person 3) - Image Generation
# Choose based on your image generation service:
# openai==1.12.0        # For DALL-E (if not already installed above)
# replicate==0.22.0     # For Stable Diffusion via Replicate

# Optional - For better PDF parsing (if PyPDF2 doesn't work well)
# pdfminer.six==20221105
# pytesseract==0.3.10   # For OCR if PDFs are scanned